$('#textBoxId').keydown(function(e) {
      if(e.keyCode== 13) {
          e.preventDefault(); 
              $('#buttonId').trigger('click');
                }
                });
if (something)
      not_good ++;
else
      alert('hi');
$('#textBoxId').keydown(function(event) {
        var e = event || window.event;
            var keyCode = document.all ? e.keyCode : e.which;
                if (keyCode == 13) {
                        e.preventDefault();
                                $('#buttonId')[0].click();
                                    }
                                    });
switch(someval){
      case a:break;
      case 'b':
             do_somethin;
             more_sts();
             end = function(magk,sure){
                   this.happens();
yeah;
             };
             if (1) {
                   print('heelo');
             }

             else 3+4;
             return;
      default:
             testit;
}
